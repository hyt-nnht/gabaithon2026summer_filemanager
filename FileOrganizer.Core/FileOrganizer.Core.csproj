<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\FileOrganizer.Shared\FileOrganizer.Shared.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- Database/: SQLite WAL永続化（HistoryRecord/AppSettings/RuleModel） -->
    <PackageReference Include="Microsoft.Data.Sqlite" Version="10.0.11" />
    <!-- Client/: Python (FastAPI) IPC通信のJSONシリアライズは System.Text.Json（.NET 10 SDKに同梱のためPackageReference不要） -->
  </ItemGroup>

</Project>
